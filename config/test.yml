server:
  address: ":8080"
datastore:
  driver: "postgres"
  address: "postgresql://kaviraj:kaviraj@localhost/bs_test?connect_timeout=10&sslmode=disable"
redis:
  address: "localhost:6392"
  dbnum: 0
